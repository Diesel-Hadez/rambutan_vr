<% provide(:title, "Edit Actor's Movie" %>
<% provide(:submit_text, "Edit Actor's Movie") %>
<% provide(:submit_url, actor_movies_path(@actor_movie)) %>
<% provide(:form_title, "Edi Actor's Movie") %>
<%= render 'form', actor_movie: @actor_movie %>

<%= link_to 'Show', @actor_movie %> |
<%= link_to 'Back', actor_movies_path %>
