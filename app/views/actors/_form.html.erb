<%= javascript_include_tag "custom" %>
<%= form_for(@actor, url: yield(:submit_url), :html => {class: "mb-4"}) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h1 class="text-center"><%= yield(:form_title) %> </h1>
  <div class="form-group">
    <%= form.label :name, class: 'form-text' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <%= form.submit yield(:submit_text), class: "btn btn-lg btn-block btn-primary" %>
<% end %>
