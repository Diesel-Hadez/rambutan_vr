<% provide(:title, "Add Movie") %>
<% provide(:submit_text, "Add Movie") %>
<% provide(:form_title, "New Movie") %>
<% provide(:submit_url, movies_path(@movie)) %>

<%= render 'form', movie: @movie %>
