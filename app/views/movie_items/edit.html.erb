<h1>Edit Movie</h1>
<% provide(:title, "Edit Movie") %>
<% provide(:submit_text, "Submit changes") %>
<% provide(:submit_url, movie_path(@movie)) %>

<%= render 'form', movie: @movie %>

<%= link_to 'Show', @movie %> |
<%= link_to 'Back', movies_path %>
