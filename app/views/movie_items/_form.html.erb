<%= form_with(model: movie_item, local: true) do |form| %>
  <% if movie_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_item.errors.count, "error") %> prohibited this movie_item from being saved:</h2>

      <ul>
      <% movie_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
